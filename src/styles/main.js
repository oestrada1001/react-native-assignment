import { StyleSheet } from 'react-native';

const DigiStyle = StyleSheet.create({
    base: {
        marginLeft: 20,
        marginRight: 20,
        marginTop: 10
    }
})

export default DigiStyle;